{
  "name": "llm-council",
  "version": "0.1.0",
  "description": "LLM Council - Ensemble Strategy Playground",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

